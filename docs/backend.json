
{
  "entities": {
    "Reading": {
      "title": "Blood Sugar Reading",
      "description": "A single blood sugar measurement at a specific time.",
      "type": "object",
      "properties": {
        "value": {
          "type": "number",
          "description": "Blood sugar level in mg/dL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the reading was taken"
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who owns this reading"
        }
      },
      "required": [
        "value",
        "timestamp",
        "userId"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/readings/{readingId}": {
      "schema": "#/entities/Reading",
      "description": "Collection of blood sugar readings",
      "rules": "allow read, write: if request.auth != null && request.auth.uid == resource.data.userId || request.auth != null && request.resource.data.userId == request.auth.uid"
    }
  }
}
